# SAM3 官方要求:
# Python 3.12 或更高版本
# PyTorch 2.7.0 或更高版本
# CUDA 12.6 或更高版本

# 基础依赖
numpy==1.26  # 与SAM3官方要求一致
pandas==2.1.0
matplotlib==3.7.1
pillow==9.5.0
scikit-learn==1.3.0
scikit-image==0.21.0
pyyaml==6.0  # 统一使用pyyaml，避免与yaml重复
opencv-python==4.8.0.76
regex==2023.10.3
ftfy==6.1.1  # SAM3依赖

# PyTorch相关 - 使用官方推荐版本
torch==2.7.0
torchvision==0.16.0
torchaudio==2.7.0

# 安装PyTorch时请使用以下命令以确保CUDA 12.6支持:
# pip install torch==2.7.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126

# 深度学习相关
transformers==4.34.0
timm==1.0.17  # SAM3官方要求的版本
segment-anything==1.0
diffusers==0.23.0
iopath==0.1.10  # SAM3依赖
huggingface_hub==0.17.3
typing_extensions==4.8.0

# 图像和视频处理
imageio==2.31.1
imageio-ffmpeg==0.4.7

# 数据加载和处理
datasets==2.14.0
tqdm==4.65.0
joblib==1.3.2

# 日志和配置
loguru==0.6.0
omegaconf==2.3.0

# 评估指标计算
scipy==1.11.3

# 可选依赖（用于特定功能）
# Zero-shot学习相关
# clip-anytorch>=2.5.0  # 注释掉，避免Python版本冲突

# 加速训练
# apex>=0.1.0  # 从GitHub安装而非PyPI: git clone https://github.com/NVIDIA/apex && cd apex && pip install -v --disable-pip-version-check --no-cache-dir --global-option="--cpp_ext" --global-option="--cuda_ext" ./

# 分布式训练
# distributed>=1.40.0  # 注释掉，避免Python版本冲突

# 可视化增强
seaborn==0.12.2
plotly==5.17.0

# 调试和开发工具
ipython==8.15.0
ipdb==0.13.13
black==23.9.0
isort==5.12.0
flake8==6.1.0

# Jupyter支持
jupyter==1.0.0
jupyterlab==3.6.3
notebook==6.5.4

# 系统监控
psutil==5.9.5

# 兼容Windows系统的依赖
pywin32==306;platform_system=="Windows"

# CUDA相关（根据实际CUDA版本选择）
# 如果使用CUDA 11.6
# torch==1.12.0+cu116
# torchvision==0.13.0+cu116
# torchaudio==0.12.0+cu116

# 如果使用CUDA 11.7
# torch==1.13.0+cu117
# torchvision==0.14.0+cu117
# torchaudio==0.13.0+cu117

# 如果使用CUDA 11.8
# torch==2.0.0+cu118
# torchvision==0.15.0+cu118
# torchaudio==2.0.0+cu118